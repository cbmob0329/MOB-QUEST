# v228

- パーティー編成・装備変更のキャラクター画像をタップすると、現在のステータスとパッシブを既存の詳細画面で確認できる。
- 武器、防具、メイン／サブフィギュア、キャンプ装備、酒場編成、訓練のキャラクター一覧、読みかけの本、A/B編成に対応。キャンプ編成の既存の詳細表示は維持。
- 名前・カードの余白は従来の選択／入替操作を維持。画像を調べるだけでは編成や装備対象を変更しない。装備一覧の名前を表示し、操作説明を追加。
- 画像のキーボード操作とラベルを追加。詳細画面を編成オーバーレイより手前に表示。

検証：`node tests/portraits-v228.cjs`（装備4タブ、酒場、本、A/B編成、キーボード、詳細表示中の選択状態維持）、`node --check js/game.js`、`git diff --check`。詳細画面の画像も目視確認。

再生成：`node scripts/build-v228.cjs`。ローカル反映のみ。
