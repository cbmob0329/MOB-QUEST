# v227

- `お城のイベント.txt` を12章の報告イベントへ反映。田舎町Ⅱと同じ立ち絵・吹き出し方式に統一し、資料の1行につき1回の会話送りに分割。強調、レコード受け渡し、ライトアームの往復、握手を追加。最後のセリフを送った1秒後に報告を確定する。
- 資料内の草原Ⅱの同一内容の重複は1回として取り込む。320px幅でも1行がはみ出さないよう文字サイズを調整。
- 埋め込みCSSの背景画像パスをHTML基準へ補正。報告背景を先読みし、読めない場合は代替背景、さらに失敗した場合は暖色の背景を表示。
- 更新されたフィギュア資料から318体のカタログを再生成。既存IDを画像パスで維持し、通常・イベントボスのタグと隣接タグ48「疾風の狩人」を反映。
- 敵の回避率低下、通常追撃、必殺技会心、MAG割合、ソウル獲得加算を実際の戦闘計算へ接続。条件付きの効果が無条件の回避・会心や固定MAGとして解釈されないよう処理。
- 資料の全角記号を正規化し、隣接タグ28の「混乱耐性+20&」は「+20%」として取り込む。原資料は変更しない。
- v226の草原報告修正を維持。既読の本イベントが未報告の章を遮らないようにする。

## 検証

- `node tests/castle-report-v227.cjs`：12章・405ステップの順序、390px/320px幅の収まり、報告確定、背景と代替表示。
- `node tests/figures-v227.cjs`：カタログ一致、条件付き効果の解釈、隣接の重複、命中・追撃・ソウル・MAG計算。
- `node tests/grass-report-v226.cjs`：テストプレイ設定からモブホーク撃破、再読込、王様への報告、砂漠解放。
- `node --check js/game.js`、`git diff --check`。
- 通常背景と画像失敗時のスクリーンショットを目視確認。

再生成は `node scripts/build-v227.cjs`。公開先への反映は行っていない。
