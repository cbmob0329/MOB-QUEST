const assert=require('node:assert/strict'),fs=require('fs'),{open}=require('./story-v172-harness.cjs');
const seed=fs.readFileSync('tests/update-v220.cjs','utf8').match(/const injection=`([\s\S]*?)`;/)[1];
const injection=seed+`
window.test225={ending:async()=>{const stage=document.createElement('section');stage.className='ending-stage-v222';stage.innerHTML='<div class="ending-cast-v222"></div><div class="ending-bubble-v222" hidden></div>';document.body.append(stage);endingCastV222(stage,['king','pink']);await stage.endingReadyV225;const img=stage.querySelector('[data-actor="pink"] img');endingCastV222(stage,['king','pink']);const reused=img===stage.querySelector('[data-actor="pink"] img');await endingSoloTransitionV225(stage);const el=stage.querySelector('[data-actor="pink"]'),r=el.getBoundingClientRect(),result={reused,ready:img.complete&&img.naturalWidth>0,x:(r.left+r.width/2)/innerWidth,y:(r.top+r.height/2)/innerHeight,curtain:!!stage.querySelector('.ending-curtain-v225')};return result;},piece:()=>{document.querySelector('.ending-stage-v222')?.remove();mobPieceBattleV96={pHand:['01','02','03','04','05'],cHand:['06','07','08','09','10'],center:2,cCenter:2,pWins:0,cWins:0,round:1,exchanged:false,uiScrollV107:0};renderMobPieceRoundV96();window.pieceRoot225=document.querySelector('.mob-piece-battle-v132');window.pieceImages225=[...pieceRoot225.querySelectorAll('img')];},pieceCheck:()=>{const ov=ensureMobPieceOverlayV96(),root=ov.querySelector('.mob-piece-battle-v132'),stats=handBattleStatsV96(mobPieceBattleV96.pHand,2,handTagEffectsV96(mobPieceBattleV96.cHand).effects);return{same:root===pieceRoot225,images:pieceImages225.every(img=>root.contains(img)),hand:mobPieceBattleV96.pHand,attack:+root.querySelector('.piece-total-v103.player [data-piece-stat-v103="attack"]').textContent,expected:stats.attack};},hit:()=>{ensureMobPieceOverlayV96().hidden=true;const b=state.battle,a=b.allies[0],e=b.enemies[0],base=calcDamage,random=Math.random;calcDamage=()=>({value:10,crit:false,miss:false});Math.random=()=>.99;try{e.hp=10000;const out=[];for(const type of ['magic','physical']){applyEnemyDamageTo(a,e,1,type);const immediate=!!enemyVisual(e.uid).closest('.enemy-unit').hitAnimationV225;renderBattle();const unit=enemyVisual(e.uid).closest('.enemy-unit');out.push({immediate,after:!!unit.hitAnimationV225,elapsed:unit.hitAnimationV225?.currentTime});}return out;}finally{calcDamage=base;Math.random=random;}}};
`;
(async()=>{const x=await open(injection);try{const {page,errors}=x;await page.evaluate(()=>test220.setup());const ending=await page.evaluate(()=>test225.ending());assert(ending.reused&&ending.ready);assert(Math.abs(ending.x-.5)<.01&&Math.abs(ending.y-.48)<.01);assert.equal(ending.curtain,false);await page.screenshot({path:'tests/v225-ending-solo.png'});await page.evaluate(()=>test225.piece());await page.locator('[data-piece-player-card-v103="0"]').click();await page.locator('[data-piece-position-v103="2"]').click();const piece=await page.evaluate(()=>test225.pieceCheck());assert(piece.same&&piece.images);assert.deepEqual(piece.hand,['03','02','01','04','05']);assert.equal(piece.attack,piece.expected);await page.screenshot({path:'tests/v225-piece.png'});await page.evaluate(()=>test220.battle());const hits=await page.evaluate(()=>test225.hit());assert(hits.every(h=>h.immediate&&h.after&&h.elapsed<360));assert.deepEqual(errors,[]);console.log('v225 PASS: reused/decoded ending art, centered Pink after black transition, in-place piece position change and recalculated stats, magic/physical hit shake survives render');}finally{await x.browser.close();}})().then(()=>process.exit(0),e=>{console.error(e);process.exit(1)});
