# v210

- モブナビの変身を、吸収・データ化・マスターの姿の再構成・場面への復帰という段階演出に変更。消滅時にもマスターの姿と文字片を表示。
- 読みかけの本のArea1・2に召喚リングと時間差の登場を追加。起動時エラーの解消により、v209の怪人ボス登場演出も正常に有効化。
- 会話用のモブリーロ表示を、先に進む冒険カーソルではなく表示中の本のシーンで判定。怪人加入で戦闘編成から外れても会話中は表示。
- 本クリア後、共通のリザルト処理と後日談処理による冒険画面への再移動を抑止。レコードルームで準備後、HOMEの冒険から出発。
- 魔王城Ⅱ Area1の停止原因を修正。v177の演出処理が全ステップの第2引数をキャラクターIDとして検索していたため、改行を含む編成説明文で無効なCSSセレクターが発生。検索を対象のhot演出だけに限定。
- パーティー編成の確認を編成画面内に表示。いいえで戻り、はいで保存・戦闘へ進行。
- v208が上書きする報酬関数を再代入可能な宣言に修正。これまで発生していた起動時の `Assignment to constant variable` を解消。

検証: `node --check js/game.js`、`node tests/update-v210.cjs`、`node tests/update-v181-static.cjs`。
ブラウザー検証では会話の文字送りを省略し、実際のイベント処理、編成ボタン、確認のやり直し、戦闘開始、Riro表示、演出生成・終了を検証。本帰還のテストはイベント終了先を模擬して共通リザルト処理の再移動がないことを確認。
旧v207/v208のテストは旧バージョンのタイトルを固定で要求するため、v210ではそのタイトル検査に失敗する。
